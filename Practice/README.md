Please install dependencies to run server  
utlilizes localhost:3000 

needs node.js installed, run commands  
```bash
npm install express body-parser bcrypt express-session

```
To Host page run  
```bash
node server.js
```  
make sure lateset version npm is installed.

